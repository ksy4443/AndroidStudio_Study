<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HelloAndroid" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.HelloAndroid.NoActionBar"> // 스타일 이름
        <item name="windowNoTitle">true</item> // 앱 바 설정(true: 안 보이기, false: 보이기)
        <item name="windowActionBar">false</item> // 앱 바 설정(true: 보이기, false: 안 보이기)
        <item name="android:windowFullscreen">true</item> // 전체 화면 설정(true: 전체 화면 보기, false: 원래대로 보기)
        <item name="android:windowContentOverlay">@null</item> // 앱 바 그림자 없애기
    </style>

</resources>